Behaviour without a [[model]], as opposed to [[Model-based]]. Usually guided purely by expected reward instead.

A model-free agent learns purely from experience.

It is hypothesised that model-free [[reinforcement learning]] happens in the [[human brain]] (see [[RL-Dopamine Hypothesis]]).

#property