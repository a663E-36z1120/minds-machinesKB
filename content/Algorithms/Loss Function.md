 A mathematical function that quantifies the difference between a model's predictions and the actual values in a dataset. Sometimes also called a "cost function".

In general, a loss function for [[Deep Learning]] should be "differentialbe" (i.e., have well defined gradients). The reward function in [[Reinforcement Learning]] can be seen as non-differentiable (and negative) loss functions.

#algorithm