Reinforcement Learning (RL) is a [[machine learning]] framework in which an agent learns to make decisions by interacting with an environment to maximise cumulative rewards over time, pioneered by [[Sutton & Barto]].

RL assumes that the agent interacts with the environment via a [[Markov Decision Process]].

#algorithm #idea